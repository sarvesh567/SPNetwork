
use_frameworks!
platform :ios, '8.0'

target 'SPNetwork_Example' do
  pod 'Alamofire', '~> 5.0.0-rc.2'
  pod 'SPNetwork', :path => '../'
  target 'SPNetwork_Tests' do
    inherit! :search_paths

    
  end
end
